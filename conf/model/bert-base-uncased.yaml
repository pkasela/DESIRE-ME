init:
  doc_model: 'bert-base-uncased'
  tokenizer: ${model.init.doc_model}
  device: 'cuda'
  aggregation_mode: 'mean'
  embedding_size: 768
  save_model: 'bert-base-uncased_best'
  query_specialize: True
  continue_train: False
  normalize: True