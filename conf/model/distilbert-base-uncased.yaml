init:
  doc_model: 'distilbert-base-uncased'
  tokenizer: ${model.init.doc_model}
  device: 'cuda'
  aggregation_mode: 'mean'
  embedding_size: 768
  save_model: 'distilbert-base-uncased_best'
  specialized_mode: 'weight'
  normalize: True
  
continue_train: False
  